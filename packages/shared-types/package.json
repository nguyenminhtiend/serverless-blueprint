{
  "name": "@shared/types",
  "version": "1.0.0",
  "description": "Shared TypeScript interfaces and types for serverless microservices",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "dev": "tsc --build --watch"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@types/aws-lambda": "^8.10.145"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "rimraf": "^6.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.6.0"
  }
}
