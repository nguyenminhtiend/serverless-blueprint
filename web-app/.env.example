# AWS Configuration
NEXT_PUBLIC_AWS_REGION=us-east-1
NEXT_PUBLIC_AWS_USER_POOL_ID=your-user-pool-id
NEXT_PUBLIC_AWS_USER_POOL_WEB_CLIENT_ID=your-client-id

# OAuth/PKCE Configuration
# Get these from CDK output after deployment
NEXT_PUBLIC_COGNITO_DOMAIN=your-cognito-domain.auth.us-east-1.amazoncognito.com
NEXT_PUBLIC_OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback
NEXT_PUBLIC_OAUTH_LOGOUT_URI=http://localhost:3000/auth/logout

# Server-side Authentication Secret
# Generate with: openssl rand -base64 32
AUTH_SECRET=generate-a-secure-32-character-random-string

# API Configuration
NEXT_PUBLIC_API_GATEWAY_URL=your-api-gateway-url
NEXT_PUBLIC_ENVIRONMENT=development

# Legacy (will be removed in Phase 7)
NEXT_PUBLIC_AWS_USER_POOL_CLIENT_SECRET=your-client-secret